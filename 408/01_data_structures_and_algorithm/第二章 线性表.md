# 一、例题
## (一)
![[Pasted image 20260901162914.png]]

Ans: C

对于 A，顺序表与一维数组在逻辑结构上可以不相同，因为顺序表一定是线性结构，一维数组不一定用来实现顺序表，一维数组只是“连续的一排存储单元”，它本身不规定元素之间的逻辑关系，一维数组可以用来实现树

> 顺序表的逻辑结构一定是线性表，但是一维数组的逻辑结构就可以有很多了
## (二)
![[Pasted image 20260901164046.png]]

Ans: A

## (三)
![[Pasted image 20260901165749.png]]

Ans: C

对于 $III$，两者的时间复杂度都是 $O(n)$

## (四)
![[Pasted image 20260901170453.png]]

### idea:
从后往前遍历（逆序遍历），两个滚动变量维护最大值和最小值

跟这几道leetcode题目有联系：
[[leetcode1299]]
[[leetcode152]]


## (五)
链式存储设计时，**结点内**的存储单元地址（ ）。 
A. 一定连续 B. 一定不连续 C. 不一定连续 D. 部分连续，部分不连续

不同结点之间的内存地址可以不连续，但是结点内部的内存地址是连续的
Ans: A

## (六)
![[Pasted image 20260903195621.png]]

对于06. 因为这里有两个指针，p指针拿着p结点的地址，不会丢失，所以可以q的next指向s，s的next指向q。Ans: C

对于07. 先对数组做快速排序/归并排序，最低时间复杂度为$O(n\log n)$，然后用尾插法建立链表，时间复杂度为$O(n)$，所以总的最低时间复杂度为 $O(n\log n)$

对于08. 先要遍历长度为m的链表，找到该链表的尾节点，然后将其next域指向另一个链表的首节点，所以时间复杂度是$O(m)$

对于09. 标识链表首节点的位置的是头指针 Ans: C

## (七)
![[Pasted image 20260903201904.png]]

对于B. 单向链表（singly linked list）的结点只存后继地址，不存前驱地址。想要删除单链表的最后一个元素，需要找到倒数第二个元素，然后将其next域设为null
Ans: B

## (八)
![[Pasted image 20260903210445.png]]

循环单链表判空的条件是 $L\to next==L$，而不是$L\to next == \& L$
Ans: C

## (九)
![[Pasted image 20260903210944.png]]
D
## (十)
![[Pasted image 20260903211013.png]]
C
上面两道题都是因为没有考虑线性表的顺序存储而错的