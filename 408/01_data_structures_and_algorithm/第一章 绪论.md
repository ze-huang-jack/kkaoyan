
# 一、例题
## (一)
![[Pasted image 20260830212713.png]]

Ans: C 有序表

顺序表是一种数据结构
## (二)
![[Pasted image 20260830212734.png]]

Ans: A

对于C，同一种逻辑结构可以有多种存储结构，比如线性表可以顺序存储，也可以链式存储。所以说，数据的逻辑结构独立于其存储结构，线性表是逻辑结构，可以用数组存，也可以用链表存

## (三)
下列算法的时间复杂度为(  )
```c
void fun(int n){
    int i=1;
    while(i<=n)
        i=i*2;
}
```

1. 初始时i=1，每次循环i=i * 2，之后$i=1,2,4,8,16, ... , 2^k$
2. 当$2^k\leq n$ 时循环停止，两边同时取以2为底的对数，即$k\leq \log_2 n$
所以时间复杂度就是 $O(\log_{2}n)$


## (四)
![[Pasted image 20260830221119.png]]

外层循环i依次取：i=1,2,3,...,n
对于每一个i，内层j从1循环到2i，内层执行2i次
总执行次数就是对i求和：

$$
T(n)=\sum_{i=1}^{n} 2i=n(n+1)
$$
Ans: A 
