# 零、一些理解

## 左旋右旋
左旋是逆时针
右旋是顺时针

左旋：向左旋转，冲突的左孩变右孩
右旋：向右选择，冲突的右孩变左孩

RL：先右旋右孩，后左旋
# 一、例题

## (一)
![[Pasted image 20260912105832.png]]
B

顺序查找有序表的成功ASL是(n+1)/2
顺序查找无序表的成功ASL也是(n+1)/2


## (二)
![[Pasted image 20260912160621.png]]
A
顺序查找：查找第i个元素，需要查找i次
所以$1\times \frac{1}{2}+2 \times \frac{1}{3} + 3 \times \frac{1}{6}=\frac{5}{3}$

## (三)
![[Pasted image 20260912160819.png]]
B
折半查找有序顺序表的时间复杂度是$O(\log_{2}n)$
二叉排序树若是平衡的话，也是$O(\log_{2}n)$；但若二叉排序树退化成单链，则是$O(n)$

## (四) 折半查找的ASL

![[Pasted image 20260912161131.png]]
A D 
关键是画出折半查找算法的判定树，12个关键字的有序表用折半查找形成的判定树高为$\log_{2}(n+1)=\log_{2} 13=4(向上取整)$
可以构建一个数组$A[0,1,2\dots12]$，根据这个画出判定树，去找查找1次的结点有多个，查找2次的结点有多少个，一直到查到$\log_{2}(n+1)$的结点有多少个（失败的ASL看失败结点的父节点到根节点中间结点的个数）
注意成功的ASL的分母是n，失败的ASL的分母是n+1，因为有n+1个失败区间

## (五)
![[Pasted image 20260912162206.png]]
B
## (六) 判断一个树可不可以为折半查找判定树

![[Pasted image 20260912162217.png]]
A
对于给定的一个有序查找表，其对应的折半查找判定树是**确定且唯一**的。

在折半查找中，如果向下取整，要么左子树的元素个数=右子树的元素个数，要么左子树比右子树少一个
如果向下取整，要么左子树的元素个数=右子树的元素个数，要么左子树比右子树多一个

这个题关键是折半判定树必须全程统一向下 / 向上取整，BCD 里子树**有的向上、有的向下**，矛盾

比如D，可以看到倒数第二层的结点都是左子树比右子树多一个结点，但是看根节点的左子树却比右子树少一个结点，所以矛盾了，这是不可能的