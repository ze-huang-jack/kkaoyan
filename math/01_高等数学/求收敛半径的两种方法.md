---
tags:
  - 数学
  - 级数
  - 幂级数
  - 收敛半径
  - 考研
aliases:
  - 收敛半径求法
  - 比值判别法与根值判别法
created: 2026-08-04T20:11:00
updated: 2026-08-04T20:11:00
---

# 一、比值判别法（达朗贝尔判别法）

设

$$
\sum_{n=1}^{\infty}u_n,\qquad u_n\neq0.
$$

若极限

$$
L=\lim_{n\to\infty}\left|\frac{u_{n+1}}{u_n}\right|
$$

存在，则

- 若 $L<1$，则级数**绝对收敛**；
- 若 $L>1$（包括 $L=+\infty$），则级数**发散**；
- 若 $L=1$，则**无法判断**，需要改用其他方法。

---

## 用于幂级数

设

$$
\sum_{n=0}^{\infty}a_n(x-x_0)^n.
$$

若

$$
\lim_{n\to\infty}\left|\frac{a_n}{a_{n+1}}\right|
$$

存在，则收敛半径

$$
\boxed{ R=\lim_{n\to\infty}\left|\frac{a_n}{a_{n+1}}\right| }
$$


---

# 二、根值判别法（柯西判别法）

设

$$
\sum_{n=1}^{\infty}u_n.
$$

令

$$
L=\limsup_{n\to\infty}\sqrt[n]{|u_n|}.
$$

则

- 若 $L<1$，则级数**绝对收敛**；
- 若 $L>1$，则级数**发散**；
- 若 $L=1$，则**无法判断**。

> [!warning] 注意
> 这里是 **$\limsup$**（上极限），不是普通极限。因为有些数列（例如奇偶项交替的数列）普通极限不存在，但上极限始终存在。

---

## 用于幂级数

设

$$
\sum_{n=0}^{\infty}a_n(x-x_0)^n.
$$

则收敛半径

$$
\boxed{ R=\frac{1}{\displaystyle\limsup_{n\to\infty}\sqrt[n]{|a_n|}} }
$$

若普通极限

$$
\lim_{n\to\infty}\sqrt[n]{|a_n|}
$$

存在，则可以直接写成

$$
\boxed{ R=\frac{1}{\displaystyle\lim_{n\to\infty}\sqrt[n]{|a_n|}} }
$$


---

级数为$\sum a_{n}x^n$
为什么对于幂级数来讲可以，$\lim_{ n \to \infty } \frac{a_{n+1}}{a_{n}}$
而对于一般的级数来讲,要判断敛散性,必须$\lim_{ n \to \infty } \frac{a_{n+1}x^{n+1}}{a_{n}x^n}$
为什么,我不理解
然后判断这些极限与1的大小关系,为什么一定是1,而不是别的数字
我对这些不理解
以及$$\sum_{n=1}^{\infty} (\frac{3+(-1)^n}{4})^n x^{2n}$$
这个级数到底算什么,不是缺项吗,为什么好像可以用幂级数的那个方法
困惑



比值法和根值法失效的反例:
$$\sum_{n=1}^{\infty} (\frac{3+(-1)^n}{4})^n $$是发散的



**判断极限的时候，可以专门研究子数列。**

如果你找到两个子数列：
一个趋于A，
一个趋于B，
而且
A≠B,
那么整个数列极限一定不存在。